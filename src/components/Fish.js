import React, { Component } from 'react';
import PropTypes from "prop-types";
import { formatPrice } from '../helpers'; // transforms float into price format $ 1.50

class Fish extends Component {

  render() {
    return (
      <li className="menu-fish">
      </li>
    );
  }
}

export default Fish;